spring:
  jpa:
    properties:
      hibernate:
       show_sql: true
       format_sql: true
       ddl-auto: validate
  application:
    name: SpringProject
  datasource:
    url: jdbc:mariadb://localhost:3306/project
    username: webstore_admin
    password: vahva_salasana
